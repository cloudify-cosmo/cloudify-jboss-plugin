[tox]
envlist=flake8,py27

[testenv]
deps =
    nose
    nose-cov
    -rdev-requirements.txt
commands=nosetests --with-cov --cov-report term-missing --cov {toxinidir} {toxinidir}/tests

[testenv:flake8]
deps =
    flake8
    -rdev-requirements.txt
commands=flake8 {toxinidir}
